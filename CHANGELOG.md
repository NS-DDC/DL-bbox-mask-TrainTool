# 변경 이력

## 1.9.0-improved.5

- 저장한 라벨을 다시 열거나 다음 이미지를 볼 때 마스크의 클래스가 현재 선택 번호를 바꾸던 문제를 수정했습니다. 디스크에서 누락된 클래스를 등록할 때도 선택을 유지합니다.
- 클래스 목록에 실제 저장 ID를 표시하고 ID 직접 선택 입력칸을 추가했습니다. YOLO ID는 0부터 시작하며 기존 단축키 `1~9, 0 → ID 0~9`는 유지합니다.
- 프로젝트 메타데이터에 클래스 ID를 명시적으로 저장하고, 기존 번호와 이름이 충돌하는 라벨은 오류로 표시합니다.
- 자동 라벨링은 전체 모델 클래스 목록으로 ID를 결정합니다. 빈 프로젝트는 모델 ID를 보존하고 기존 프로젝트는 이름으로 기존 ID에 매핑하며, 실행 전 대응표를 표시합니다.
- 기존 마스크를 열어 보는 동작에서 불필요한 실행 취소 기록을 제거했습니다. 마스크 수정·전체 지우기를 하나의 작업으로 저장하고 실행 취소할 수 있습니다.
- 저장 직후 같은 모드에서 마스크를 계속 수정하거나 지울 수 있도록 편집 버퍼를 유지합니다.
- 클래스 전환 중인 폴리곤의 ID 변경, Detection 폴리곤의 Enter 완료 누락, 다른 이미지에 숨김 상태가 이어지는 문제를 수정했습니다. 색상·언어 변경 후에도 클래스 선택을 유지합니다.
- 검증과 EXE 빌드는 GitHub Windows 러너에서만 수행합니다. 기존에 잘못 저장된 번호는 의도를 알 수 없어 자동으로 재분류하지 않습니다.

## 1.9.0-improved.4

- 이미지 선택·탐색만으로 빈 라벨 파일이 생기던 문제를 수정. 마지막 라벨을 실제로 삭제한 경우에는 빈 TXT를 저장해 삭제 상태를 유지합니다.
- 불러온 상태와 저장이 필요한 편집 상태를 분리하고, 변경이 없는 기존 라벨·마스크는 다시 쓰지 않습니다.
- 선택 모드에서 우클릭 드래그 화면 이동, 모든 모드에서 `Space+좌클릭 드래그` 화면 이동을 추가했습니다.
- 분할 브러시의 우클릭 지우기와 폴리곤 우클릭 완료는 그대로 유지합니다.

## 1.9.0-improved.3

VisionAce v1.8.1의 원본 좌표 라벨링, 자동 라벨링, 저장 안정성 및 Windows 배포를 개선한 별도 버전입니다.

- 원격 화면 검토에서 발견한 글꼴 누락을 수정: Noto Sans KR을 번들하고 한글·ASCII 글리프 검사 추가. `.1`은 배포 전 중지, `.2`는 한글 설치 경로의 글꼴 로딩 검사에서 실패하여 게시하지 않았습니다. `.3`은 글꼴 바이트를 직접 등록합니다.

- 원본 보존을 기본 저장 방식으로 적용하고, 원본 복사는 명시적인 설정으로 분리.
- 원본 픽셀 좌표 유지, TIFF·한글 경로 지원, 1:1 및 화면 맞춤 제공.
- 모델 이름에 의존하는 RT-DETR 판별 제거; 자동 구조 판별 및 직접 형식 선택.
- 추론 실패를 성공한 빈 결과로 처리하지 않도록 변경.
- 마스크의 원본 좌표·패딩 복원, 중복 라벨 생성 방지, 같은 클래스 마스크 병합.
- 기존 라벨 보호, 프로젝트별 클래스 ID 유지, 빈 라벨 저장, 이미지 단위 실행 취소.
- 모델 비동기 로드와 작업 취소·썸네일 스레드 수명 개선.
- 실패한 저장 후 이동·종료 방지, F 원본 삭제 동작을 세션 숨김으로 변경.
- 외부 라벨 가져오기 충돌과 클래스 매핑 검사.
- GitHub Windows 회귀 검사·exe 빌드·기동 검사·ZIP 릴리스 자동화.

실제 모델 추론, GPU 성능, 초대형 이미지 성능은 미검증입니다. DINOv3 백본 단독 추론을 지원한다고 표시하지 않습니다.
